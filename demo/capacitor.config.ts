import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.passkeykit.capacitordemo',
  appName: 'Passkey Kit Capacitor Demo',
  webDir: 'dist'
};

export default config;
